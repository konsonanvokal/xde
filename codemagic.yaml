workflows:
  snapcraft-release:
    name: Snapcraft Release
    instance_type: linux_x2
    triggering:
      tag_patterns:
         - pattern: '*-test'
           include: true
    scripts:
      - name: Authenticate
        script: |
          nproc
